import {color, accentColor} from './color'
import {typography} from './typography'
const theme = {
  color,
  accentColor,
  typography,
  shadow: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 10,
    },
    shadowOpacity: 0.53,
    shadowRadius: 13.97,

    elevation: 21,
  }
}

export default theme